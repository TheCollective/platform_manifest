AOCP JELLYBEAN

$ repo init -u https://github.com/xAOCPx/platform_manifest.git -b jellybean
$ repo sync

Or if you have slow internet

$ repo sync -j1

# To use CCACHE
    sudo apt-get install ccache

# Then open bashrc
    sudo gedit bashrc
# Add this line to the bootom of bashrc
    export USE_CCACE=1

# Then cd into your working directory, type in terminal
    prebuilt/linux-x86/ccache/ccache -M 50G
